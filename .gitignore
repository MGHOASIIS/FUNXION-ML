# ── Python / editor artefacts ─────────────────────────────────────────────────
.claude/
.venv/
.pytest_cache/
__pycache__/
*.pyc
*.pyo
.DS_Store
*.egg-info/

# ── Storage — heavy / private data, never push to git ─────────────────────────
# Raw recordings, pickled datasets, and all results live under storage/.
# Set XDASH_STORAGE_DIR env var to relocate (e.g. HPC scratch).
storage/

# Longitudinal test sessions
test_sub_NM/
tests/

# ── Binary ML artefacts ───────────────────────────────────────────────────────
*.pkl
*.pt
*.h5
*.hdf5

# ── Misc development files ────────────────────────────────────────────────────
environment.yml
documentation/
tem.txt
*.tmp